<!DOCTYPE html>
<html lang="en">
<head>
 <!-- Define the character encoding and page title -->
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <title>Frosted Glass Effect</title>
 <!-- Define the CSS styles for the page -->
 <style>
 // Set the styles for the body and html elements
 body, html {
 margin: 0;
 padding: 0;
 width: 100%;
 height: 100%;
 font-family: Arial, sans-serif;
 display: flex;
 align-items: center;
 justify-content: center;
 // Set the image as the background for the body and html elements
 background: url('https://source.unsplash.com/random/1920x1080') no-repeat center center fixed;
 background-size: cover;
 }
 // Set the styles for the frosted-glass element
 .frosted-glass {
 width: 80%;
 max-width: 800px;
 padding: 20px;
 background: rgba(255, 255, 255, 0.2); /** Set the background color to white with an alpha value of 0.2 /
 border-radius: 10px;
 backdrop-filter: blur(10px); /* Apply the blur effect to the backdrop of the element /
 -webkit-backdrop-filter: blur(10px); /* Apply the blur effect for webkit-based browsers /
 color: #fff; /* Set the text color to white /
 text-align: center; /* Center-align the text /
 box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Apply a light shadow to the element */
 }
 // Set the styles for the h1 element inside the frosted-glass element
 .frosted-glass h1 {
 margin: 0 0 10px 0; /** Remove the top margin and set the bottom margin to 10px /
 font-size: 2.5em; /* Set the font size to 2.5em */
 }
 // Set the styles for the p element inside the frosted-glass element
 .frosted-glass p {
 font-size: 1.2em; /** Set the font size to 1.2em */
 }
 // Set the styles for the footer element inside the frosted-glass element
 .footer {
 margin-top: 20px; /** Set the top margin to 20px /
 font-size: 0.9em; /* Set the font size to 0.9em */
 }
 </style>
</head>
<body>
 <!-- Define the frosted-glass element and its content -->
 <div class="frosted-glass">
 <h1>Frosted Glass Effect</h1>
 <p>
 Recent advances in web technology have allowed the easy implementation of the frosted glass effect on websites. The blurry appearance of elements behind the frosted glass overlay helps add color to an area while also allowing text or objects to appear over the image and remain readable.
 </p>
 <p>
 The effect has become a popular option in a designer’s tool belt and has increasingly been used as a background in place of gradients.
 </p>
 <div class="footer">
 <p>Made by Benjamin Moon</p>
 </div>
 </div>
</body>
</html>
